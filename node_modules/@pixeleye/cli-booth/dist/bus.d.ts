export type Bus<T> = {
    add: (message: T) => Promise<void>;
    hurryAndWait: () => Promise<void>;
};
export declare function createBus<T>(options: {
    delay: number;
    batchSize: number;
    handler: (messages: T[]) => Promise<void>;
}): Bus<T>;
//# sourceMappingURL=bus.d.ts.map